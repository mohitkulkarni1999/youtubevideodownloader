# Server Port
server.port=8080

# Logging Configuration
logging.level.root=INFO
logging.level.com.example.youtubedownloader=DEBUG

# File Upload/Download Settings (if needed)
# Specify a directory for temporary files
# file.upload-dir=./uploads

# Configure database settings if you plan to add database functionality
# spring.datasource.url=jdbc:mysql://localhost:3306/yourdb
# spring.datasource.username=root
# spring.datasource.password=password
# spring.jpa.hibernate.ddl-auto=update

# Configure server context path if needed
# server.servlet.context-path=/your-app

# Other custom properties
# my.custom.property=value
